\ Kevo object library\ (c) Antero Taivalsaari 1992 \ Object provides the common operations which are needed for\ (almost) all the objects in the system.Prototypes CDREF Object<mkdir> -> ObjectObject ADDS\ Printing operations  : printId	." <id: " self . ." >" ;  : printContents	printId ;  : printType	self object>typename IF name'name @ type space ELSE ." (no type) " THEN ;  : printString	." (" self.printType self.printContents ." )" ;  : printInterface 	self names ;  : printImage 	mirror ;\ Creation operations	\ generic sharing (shallow copying) operation. This should never be overridden.  : share    self <shallowCopy> ;        \ generic shallow copying operation. This can be overridden when needed.  : clone	self <shallowCopy> ;		\ generic deep copying operation. Overridden when needed.  : deepCopy		self <shallowCopy>		area# 1- 0 DO 			area0 i cell* + @ dup hasContext 			IF " deepCopy" over respondsTo 				IF top.deepCopy over >area i cell* + ! 				ELSE drop				THEN			ELSE drop 			THEN 		LOOP ;	\ generic initialized object creation. Overridden when needed.  : new	self.clone ;	\ generic object deletion operation. Overridden when needed.  : dispose	self <dispose> ;\ Browsing operations & utilities  : browse  	self browse ;  : respondsTo	( string -- flag )  	\ check if the object is capable of responding to the requested message  	self respondsTo ;  	\ Error handling  : traceBack 	."  in " self.printType self.printId ;ENDADDS;