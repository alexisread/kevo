\ Kevo  (c) Antero Taivalsaari 1992\ ---------------------------------------------------------------------\ Dynamically growing array structurePrototypes CDDEFINED Collections not IF MKDIR Collections THENCollections CDREF ArrayMemory.clone -> ArrayArray ADDS\ Internal operations  : ?grow	( index --  )	\ If needed, grow the storage space by four cells	dup one area# between	IF drop ELSE 4 + resize THEN ; hidden  : position	( index -- address )	1- cell* area0 + ; \ Access operations  : fetch	( index -- item )	dup one area# between	IF position @ ELSE drop 0 THEN ;  : store	( item index --  )	dup one < IF ." -- Out of range" error THEN	dup ?grow position ! ;  : count	(  -- size )	area# ;\ Printing operations  : printContents	self.count TIMES          i self.fetch .	LOOP ;ENDADDS;