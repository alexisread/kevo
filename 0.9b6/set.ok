\ Kevo  (c) Antero Taivalsaari 1992\ ------------------------------------------------------------------------Prototypes CDDEFINED Collections not IF MKDIR Collections THENCollections CDREF SetList.clone -> SetSet ADDS\ Access operations  : add		( n --  )	dup find IF drop ELSE add THEN ;  : member	( n -- flag )	find 0<> ;  : delete	notAvailable ;  : insert	notAvailable ;  : union 	( aSet -- aSetUSelfSet )	{ TEMP result		top.clone -> result		self.count TIMES		  i self.fetch result.add		LOOP result	} ;  : intersect	( aSet -- aSet*SelfSet )	{ -> TEMP otherSet  	  TEMP result		self.new -> result		self.count TIMES			i self.fetch dup otherSet.member 			IF   result.add 			ELSE drop 			THEN		LOOP result	} ;  : difference	( aSet -- aSet-SelfSet ) 	{ TEMP result		top.clone -> result		self.count TIMES			i self.fetch dup result.member			IF   result.remove 			ELSE drop			THEN		LOOP result 	} ;  : compare		( aSet -- 0</0=/0> ) 	{ -> TEMP otherSet			otherSet.count self.count - dup		0= IF drop 0			  self.count TIMES 			  	i self.fetch dup otherSet.member not				IF drop i leave THEN			  LOOP		THEN	} ;  : isEqual		( aSet -- flag )  	self.compare 0= ;			ENDADDS;